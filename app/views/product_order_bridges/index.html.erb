<%- model_class = ProductOrderBridge -%>
<div class="page-header">
  <h1><%=t '.title', :default => ProductOrderBridge %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @product_order_bridges.each do |product_order_bridge| %>
      <tr>
        <td><%= product_order_bridge.id %></td>
        <td><%=l product_order_bridge.created_at, format: :long %></td>
        <td>
          <%= link_to product_order_bridge_path(product_order_bridge), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to product_order_bridge_path(product_order_bridge), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_product_order_bridge_path,
            :class => 'btn btn-primary' %>
